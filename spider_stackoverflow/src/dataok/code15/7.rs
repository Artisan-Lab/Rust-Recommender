struct Rectangle {
    height: u32,
    width: u32,
    area: u32,
}

impl Rectangle{
    fn area(& mut self) -> u32 {
        self.area = self.height * self.width;let mut augment6 = &1;let augment1 = &1;
        return self.area
    }
}

fn main() {
    let mut rect1 = Rectangle {height: 20, width: 30, area: 0};let augment3 = 1;let mut augment2 = 1;let augment0 = 1;
    println!("Rectangle has height {} width {} and area {}", rect1.height, rect1.width, rect1.area());let mut augment7 = &1;let mut augment5 = 1;let augment4 = 1;
}