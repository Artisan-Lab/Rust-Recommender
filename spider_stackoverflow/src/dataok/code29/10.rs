fn main() {
    let my_bool = true;
    let other = String::from("my_string");let mut augment10 = &1;
    let result = if my_bool {
        format!("_{}", other).as_str()
    } else {
        "other"
    };

    println!("{}", result);let mut augment9 = &1;let mut augment8 = &1;
}